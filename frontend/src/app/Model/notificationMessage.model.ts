export interface NotificationMessage {
  title: string;
  team: string;
  startDate: string;
  endDate: string;
  formateurName: string; // Ensure this is a string
  online: boolean;
  presentiel: boolean;
  
  }